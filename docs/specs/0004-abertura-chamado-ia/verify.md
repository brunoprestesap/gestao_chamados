# Verify: Abertura do chamado pela IA · spec 0004 · updated 2026-09-22 (quinta passada, pós-deploy)

_Steps derived from spec 0004 acceptance criteria. `/check verify` runs these; `/test` locks the durable ones._

## UI / manual

- [x] Entrar como `solicitante`, abrir `/conversas`, mandar "A lâmpada da sala 204, no 2º andar, queimou" → a resposta chega aos poucos e, quando a IA julga completo, aparece o cartão `Resumo do chamado` com o serviço do catálogo, a unidade do perfil com o andar e o local "sala 204" → AC-1, AC-4, AC-5, AC-6. **Verificado em 21/09/2026** contra o vLLM real: chamado `#CHM-2026-00664`, serviço `TROCA DE LÂMPADAS` (Elétrica · Manutenção Predial), unidade `NUTEC - NÚCLEO DE TECNOLOGIA · 1º ANDAR` pré-preenchida.
- [x] No cartão do passo anterior, conferir que não aparece confiança, motivo nem prioridade, e que o texto diz que a conversa vira a descrição → AC-5. Conferido também na linha de `/gestao`, no diálogo `Classificar` e no `/meus-chamados/<id>` (Prioridade Final sempre "—").
- [x] Trocar a unidade na lista (só unidades ativas, com andar) e editar o local, depois `Confirmar e abrir chamado` → a mesma rota vira modo leitura, a lateral troca o rascunho pelo chamado, o título é `<nome do serviço> — <local>` e a última mensagem é o aviso verde "Chamado #… aberto" sem link do formulário → AC-6, AC-10, AC-11. **Verificado em 21/09/2026** (segunda passada, com uma segunda unidade ativa semeada localmente): relato "interruptor da sala 105", cartão veio pré-preenchido com a unidade do perfil (NUTEC), troquei para `DIREF - DIVISAO DE RECURSOS FINANCEIROS · 3º ANDAR` e editei o local para "sala 105, perto da porta (editado no verify)"; chamado `#CHM-2026-00667` nasceu com esse título, modo leitura e aviso verde sem link do formulário. Conferido no Mongo: `chamados.unitId` do `#CHM-2026-00667` é o `_id` da unidade DIREF, não o NUTEC do perfil — a troca realmente persiste.
- [x] No modo leitura do passo anterior, conferir a marca `Aberto pelo chat · serviço sugerido pela IA` no cabeçalho e, no histórico, uma entrada de abertura e uma `decisao_ia` de serviço, sem nenhuma entrada de prioridade → AC-10, AC-15
- [x] Entrar como `preposto`, abrir `/gestao` → a linha do chamado mostra a marca do chat (cortada com reticências, texto inteiro ao passar o mouse); abrir `Classificar` → a marca aparece no cabeçalho do diálogo e nada mostra a prioridade sugerida → AC-15. **Verificado em 21/09/2026**: o `<span>` da marca no `#CHM-2026-00665` tem `class="truncate"` (`overflow: hidden`, `text-overflow: ellipsis`, `white-space: nowrap` via `getComputedStyle`) e o `<span>` pai tem `title="Aberto pelo chat"` (tooltip nativo do navegador com o texto inteiro). No diálogo `Classificar` desse chamado (sem IA), o cabeçalho mostra "Aberto pelo chat" e o corpo diz "Serviço: A definir na triagem", sem prioridade sugerida.
- [x] Abrir `/meus-chamados/<id>` do chamado → marca `Aberto pelo chat · serviço sugerido pela IA` abaixo do título; um chamado aberto pelo formulário não mostra marca → AC-15. **Verificado em 21/09/2026**: abri `#CHM-2026-00671` pelo formulário tradicional (`/meus-chamados`, diálogo `Novo Chamado`) e conferi por script que nem a linha da tabela nem `/meus-chamados/<id>` contêm o texto "Aberto pelo chat" — ausência confirmada, não só a presença nos chamados do chat.
- [x] Entrar como `tecnico` com um chamado do chat atribuído → a tela do técnico não mostra marca → AC-15. Classifiquei e atribuí `#CHM-2026-00664` a `Técnico 01 E2E`; nem o card do dashboard nem `/chamados-atribuidos/<id>` mostram marca.
- [x] Usuário sem unidade no perfil manda um relato completo → a IA pergunta onde é, o cartão chega com a unidade vazia e obrigatória, e confirmar sem escolher mostra "Escolha a unidade" ligado ao campo → AC-2, AC-6. **Verificado em 21/09/2026** (segunda passada, com o usuário de fixture `solicitante.semunidade` semeado sem `unitId`, aprovado pelo usuário): relato "a tomada da sala 105 está soltando faísca", a IA pediu para confirmar o local (não a unidade, mas a unidade nunca tinha sido mencionada); `Revisar e abrir` montou o cartão com `Unidade *` vazio (`Escolha a unidade`); clicar `Confirmar e abrir chamado` sem escolher deixou o combobox `aria-invalid` com o texto "Escolha a unidade" abaixo do campo; escolhendo a unidade o erro sumiu e o chamado `#CHM-2026-00666` nasceu normalmente.
- [x] Relatar problema em outro prédio ("a lâmpada da garagem do anexo queimou") com unidade no perfil → o cartão não pré preenche a unidade do perfil → AC-6. **Verificado em 21/09/2026**: como `solicitante` (unidade de perfil NUTEC), relatei "A lâmpada da garagem do anexo queimou, ela fica no subsolo do prédio anexo, e `Revisar e abrir` montou o cartão com `Unidade *` vazio (`Escolha a unidade`), sem pré-preencher NUTEC.
- [x] Depois da primeira mensagem, com a IA ainda sem cartão, tocar `Revisar e abrir` → aparece o cartão (modo `ia` se a proposta tem serviço, `manual` se não tem) e o foco continua no botão; tocar de novo sem mudar nada → nenhum cartão novo nasce → AC-7, AC-18. **Verificado em 21/09/2026**: modo `ia` confirmado (idem à passada anterior). Idempotência confirmada de forma indireta e mais forte que um clique duplo: numa conversa (`vaso sanitário`, `#`s de teste), cliquei `Revisar e abrir`, depois renomeei o serviço no catálogo por fora e cliquei de novo, e ainda dei um reload completo da página — em nenhum desses casos nasceu um segundo `ConversaMensagem` do tipo `cartao` (só 1 no Mongo); só depois de uma mensagem nova de verdade (que mudou o `completo`) é que um segundo `cartao` foi gravado.
- [x] Mandar outra mensagem que muda o local → nasce um cartão novo e o anterior fica esmaecido com a marca `Substituído` em texto e sem botão → AC-4, AC-12. **Verificado em 21/09/2026**: na mesma conversa do vaso sanitário, depois de renomear `HIDR-001` no catálogo e mandar "É perto da porta do banheiro mesmo, sala 401", nasceu um segundo `ConversaMensagem` tipo `cartao` com o `rotuloServico` já atualizado (`REPARO DE VAZAMENTO (RENOMEADO VERIFY)`); no DOM o `<article>` antigo ficou com `opacity-60 saturate-50`, texto "Substituído / Um resumo mais novo tomou o lugar deste." e sem nenhum `<button>` de confirmar.
- [x] Com dois cartões na tela, abrir a mesma conversa noutra aba, gerar um cartão novo lá e tentar confirmar o antigo na primeira → frase "Este resumo não vale mais…" como alerta, nenhum chamado criado → AC-10, AC-12. **Verificado em 21/09/2026**: abri a mesma conversa em duas abas, gerei um cartão novo na aba 2 ("Na verdade é o ralo do chuveiro... não a pia") e, na aba 1 (ainda com o cartão antigo "REPARO DE VAZAMENTO" na tela), cliquei `Confirmar e abrir chamado`. Apareceu o alerta "Este resumo não vale mais, porque a conversa mudou depois dele. Use o resumo mais recente ou toque em Revisar e abrir." e o Mongo confirma que nenhum `Chamado` foi criado para essa conversa a partir desse clique.
- [x] Enquanto a resposta do assistente chega, o botão `Confirmar e abrir chamado` fica desabilitado e diz por quê → AC-12. **Verificado em 22/09/2026** (terceira passada), com o DOM observado por polling a cada 150ms em vez de leitura de código: no rascunho da pia/ralo do banheiro 501 (`#6ab1622840a587facb5a38fa`), com o cartão vigente já na tela, mandei uma mensagem nova ("Ok, obrigado") e comecei a amostrar o botão imediatamente. Do envio até por volta de 1,1s depois, `button.disabled === true` com `aria-describedby` apontando para o texto de espera (`CARTAO_ESPERE_RESPOSTA`); assim que a resposta terminou, o mesmo cartão (conteúdo igual, não virou "Substituído") voltou com `disabled === false`. Mecanismo em `CartaoResumo.tsx:144`/`:445-447` confirmado batendo com o DOM real durante o streaming, não só lido no código.
- [x] Achado em 22/09/2026 (terceira passada), corrigido no mesmo dia → AC-14. Na mesma volta acima, a resposta do modelo (vLLM real) foi "O chamado foi aberto para reparo de vazamento no ralo do chuveiro da sala 501. Obrigado por reportar.", mesmo com o rascunho continuando `Rascunho` (nenhum chamado nasceu; o cartão seguiu pedindo confirmação). O prompt já trazia a regra "nunca dizer... que o chamado foi aberto" (`prompt-schema.test.ts` confere que o texto da regra existe), mas isso não impedia o modelo de violar a instrução na prática. **Corrigido em `/debug` (22/09/2026)**: segunda trava, que não depende do modelo obedecer. `afirmaChamadoJaAberto` e `respostaSemAfirmarAbertura`, novas em `lib/assistente/mensagens.ts`, detectam frases que só são verdade depois da confirmação (chamado aberto, criado, registrado, com número ou protocolo, ou um código `CHM-AAAA-NNNN` inventado) e trocam a `resposta` do modelo pela frase fixa do Sigma antes de ela virar quadro `fim` e mensagem gravada, em `lib/assistente/responder.ts`. A extração (serviço, local, `completo`) não muda, só o texto mostrado. **Reverificado ao vivo contra o vLLM real** na mesma conversa (`#6ab1622840a587facb5a38fa`): mandei outra mensagem neutra ("Certo, muito obrigado mesmo") e o modelo repetiu o mesmo tipo de afirmação; desta vez a tela mostrou a frase fixa do Sigma ("Entendi. Nenhum chamado foi aberto ainda: confira o resumo abaixo...") no lugar do texto do modelo, o log trouxe `[assistente] resposta afirmava chamado aberto {"conversaId":"..."}` sem nenhum texto de relato ou resposta, e a conversa continuou `Rascunho` com o cartão ainda pedindo confirmação. Regressão coberta por 4 testes novos em `lib/assistente/__tests__/responder.test.ts` e 6 em `lib/assistente/__tests__/mensagens.test.ts`, com o texto exato reproduzido como fixture.
- [x] Com `LLM_ENABLED=false`, mandar um relato → aparece a mensagem de reserva (relato salvo, resumo abre mesmo assim, formulário como alternativa) e logo depois o cartão manual; escolher o tipo, a unidade e o local e confirmar → o chamado nasce com `tipoServico` escolhido, sem serviço do catálogo, e o detalhe mostra "A definir na triagem" → AC-8, AC-9. **Verificado em 21/09/2026** reiniciando o `next dev` com `LLM_ENABLED=false`: chamado `#CHM-2026-00665`, badge `Aberto pelo chat` (sem "serviço sugerido pela IA"), detalhe mostra `Elevador · A definir na triagem`.
- [x] Classificar como Preposto o chamado do passo anterior → o diálogo mostra "Serviço: A definir na triagem" e só salva com subtipo e serviço escolhidos → AC-9. **Verificado em 21/09/2026**: entrei como `preposto`, abri `Classificar` no `#CHM-2026-00665` — o diálogo mostrava "Serviço: A definir na triagem. Escolha o subtipo e o serviço abaixo." com `Subtipo *` e `Serviço do Catálogo *` vazios; cliquei `Classificar Chamado` sem escolher nenhum dos dois e o diálogo continuou aberto sem salvar (confirmado no Mongo: `status` seguiu `aberto`, `catalogServiceId`/`subtypeId` continuaram `null`).
- [x] Conversa com 30 mensagens → a caixa some e aparece o aviso com `Revisar e abrir` como ação principal e `Abrir por formulário` como alternativa; o chamado nasce pelo cartão → AC-13. Observado ao vivo num rascunho já no teto (fixture de 18/09); o texto e os botões batem com a spec. O teto não foi atingido enviando mensagens nesta sessão.
- [ ] Com leitor de tela (NVDA): quando a resposta termina com cartão novo, a região ao vivo lê uma vez só a resposta seguida de "Resumo do chamado pronto para confirmar."; pelo `Revisar e abrir`, só essa frase; todo campo do cartão tem rótulo lido e o erro é anunciado junto → AC-18. **Bloqueado**: sem NVDA neste ambiente. Conferi programaticamente o conteúdo da região `status`: cartão novo → resposta completa + frase; via `Revisar e abrir` → só a frase. Bate com o texto esperado.
- [x] No celular (390 de largura), o cartão cabe sem rolagem horizontal e todo alvo tem 44 pixels → AC-18. **Achado em 21/09/2026**, corrigido e reverificado em 22/09/2026: sem rolagem horizontal confirmado (`scrollWidth === clientWidth === 375` a 390px de viewport). Os elementos do próprio cartão (`select` de unidade, campo de local, botão `Confirmar e abrir chamado`) já mediam 44×260px — ok. O achado original eram os dois controles fixos do cabeçalho mobile (`components/dashboard/mobile-header.tsx` e `components/realtime/NotificationsBell.tsx`), com 36×36px (`h-9 w-9`). **Corrigido em `/debug` (22/09/2026)**: `h-9 w-9` → `h-11 w-11` nos dois arquivos, com teste novo em `components/dashboard/__tests__/mobile-header.test.tsx`. Reverificado ao vivo por `getBoundingClientRect()` a 390px de largura em `/dashboard` e em `/conversas`: os dois botões ("Abrir menu de navegação" e "Notificações — N não lidas") agora medem 44×44px nas duas telas, e `scrollWidth === clientWidth === 375` continua batendo (sem rolagem horizontal nova).
- [x] Os quatro perfis (Admin, Preposto, Solicitante, Técnico) conseguem abrir chamado pelo chat, sempre em nome de quem está logado → AC-17. **Verificado em 21/09/2026**: abri um chamado pelo chat logado como cada um dos 4 perfis nesta sessão e conferi no Mongo que `chamados.solicitanteId` bate com o `_id` de quem estava logado em cada caso — `solicitante` (`#CHM-2026-00666`, `#CHM-2026-00667`), `preposto` (`#CHM-2026-00668`), `admin` (`#CHM-2026-00669`), `tecnico` (`#CHM-2026-00670`).

## Origem dos valores (uma verificação por linha da tabela)

- [x] Desativar um serviço no catálogo e mandar um relato que caberia nele → a IA não o escolhe (o catálogo é lido a cada chamada) → Value sourcing: catálogo no prompt. **Verificado em 21/09/2026** com `LLM_DEBUG=true`: desativei `ELET-002` (REPARO DE TOMADAS), mandei "a tomada da sala 310 não está funcionando", e o bloco de catálogo no `system` prompt enviado ao vLLM real não continha mais a linha `ELET-002`; a IA devolveu `ELET-004` (REPARO DE DISJUNTOR) em vez de tomadas. Reativei o serviço depois.
- [x] Desativar a unidade do perfil do usuário → o prompt diz que não há unidade e o cartão pede a unidade → Value sourcing: unidade do perfil. **Verificado em 21/09/2026**: desativei a unidade NUTEC (perfil do `solicitante`), mandei "o vaso sanitário da sala 401 está vazando", e o prompt real (`LLM_DEBUG`) trouxe "Sobre quem está relatando: - A pessoa não tem unidade no perfil. Se o relato não disser onde é, pergunte." (idêntico ao texto de um usuário sem `unitId`); `Revisar e abrir` montou o cartão com `Unidade *` vazio e obrigatório, listando só a unidade ainda ativa (DIREF). Reativei a NUTEC depois.
- [x] Segunda mensagem da mesma conversa → no registro `LlmCall`, o prompt leva a proposta atual (código e local) → Value sourcing: proposta atual no prompt. **Verificado em 21/09/2026** com `LLM_DEBUG=true`: na 2ª chamada de uma conversa (porta do banheiro), o `system` prompt real trazia "O que você já tinha entendido nesta conversa: - Serviço: CIVI-001. - Local exato: banheiro do 2º andar." (exatamente o que a 1ª resposta tinha decidido).
- [x] Conversa com mensagens de sistema e cartões → nada disso vai no histórico enviado ao modelo → Value sourcing: histórico enviado. **Verificado em 21/09/2026** por log real (`LLM_DEBUG`): em ambas as chamadas capturadas, o array `messages` só tinha entradas `{role: "user"|"assistant", content: <texto>}`, nunca um quadro `cartao` ou mensagem de `sistema`. Não dá para forçar um caso com mensagem de sistema/cartão já presentes ANTES de uma nova chamada ao modelo, porque depois que o cartão nasce e o chamado é confirmado a conversa vira modo leitura e não há mais chamada à IA nessa conversa — a exclusão é estrutural, não só observada por sorte.
- [x] `propostaIa.origemMensagemId` no Mongo é o `_id` da mensagem do solicitante da última volta boa → Value sourcing: `origemMensagemId`. **Verificado em 21/09/2026**: no `Conversa` do disjuntor, `propostaIa.origemMensagemId` apontava exatamente para o `_id` do `ConversaMensagem` do solicitante daquela mesma volta ("O disjuntor do quadro da sala de manutenção está desarmando sozinho").
- [x] `propostaIa.servico` tem os ids do `ServiceCatalog` cujo `code` a IA devolveu, e `tipoServico` sai do nome do tipo → Value sourcing: `propostaIa.servico`. **Verificado em 21/09/2026**: cruzei `propostaIa.servico.catalogServiceId`/`subtypeId` de vários cartões com os documentos reais de `ServiceCatalog`/`ServiceSubType` no Mongo — batem (ex.: `#CHM-2026-00667` → `ELET-002`/Elétrica).
- [x] Forçar motivo vazio (servidor falso) → a decisão grava "O modelo não explicou." → Value sourcing: confiança e motivo. **Fechado na quarta passada (22/09/2026)**, por teste automatizado com evidência própria: `lib/assistente/__tests__/responder.test.ts`, teste `troca motivo vazio do modelo pela frase fixa` (fluxo falso com `servicoMotivo: '  '` e `prioridadeMotivo: ''`), roda de verdade a cada `npm test` e passou nesta sessão junto com o resto da suíte (2215 passaram).
- [x] `propostaIa.llmCallId`, `modelo`, `promptVersion` e `task` batem com o `LlmCall` da mesma volta → Value sourcing: `meta`. **Verificado em 21/09/2026**: `propostaIa.llmCallId` do `#CHM-2026-00667` (`6ab15e061356f6f2b4efa455`) é o `_id` exato de um documento em `llmcalls` com `model: "qwen3-8b"`, `promptVersion: "1"`, `task: "conversa.abertura"` — os três batem com o que `propostaIa` guardou.
- [x] Proposta completa sem local → nenhum cartão nasce sozinho → Value sourcing: decidir se grava cartão. **Fechado na quarta passada (22/09/2026)**: já existe `lib/assistente/__tests__/responder.test.ts`, teste `não grava cartão sem local, mesmo com serviço e completo` (proposta com `localExato: null`, serviço válido e `completo: true`), que confere `mockGravarCartao` não chamado e nenhum quadro `cartao` no fluxo; rodei esse teste nesta sessão e ele passou.
- [x] Renomear o serviço no catálogo depois do cartão e tocar `Revisar e abrir` → o cartão novo mostra o nome novo → Value sourcing: rótulo do serviço e do subtipo. **Verificado em 21/09/2026**: renomear `HIDR-001` de "REPARO DE VAZAMENTO" para "REPARO DE VAZAMENTO (RENOMEADO VERIFY)" e mandar uma mensagem nova (não só re-clicar no botão, que é idempotente — ver item de AC-7/AC-12 acima) fez nascer um `cartao` novo com `payload.servico.rotuloServico` já no nome novo.
- [x] Lista de unidades do cartão = unidades ativas, em ordem de nome → Value sourcing: lista de unidades. **Verificado em 21/09/2026**: com 2 unidades ativas (`DIREF...`, `NUTEC...`), o `<select>` do cartão listava nessa ordem alfabética (D antes de N); ao desativar a NUTEC, sumiu da lista; ao reativar, voltou.
- [x] Cartão sem serviço, sem unidade e sem local → `faltando` = tipo, unidade, local → Value sourcing: `faltando[]`. **Fechado na quarta passada (22/09/2026)**: novo teste em `lib/assistente/__tests__/cartao.test.ts`, `sem proposta e sem unidade no perfil, falta tipo, unidade e local juntos` (`montarCartao({ proposta: null, perfil: { unidade: null } })`), confirma `faltando: ['tipo', 'unidade', 'local']` na ordem certa. `montarCartao` é função pura, então o teste roda o código de verdade, sem precisar de IA nem de banco.
- [x] Título do chamado: nome do serviço no modo `ia`, tipo no modo `manual`, sempre com " — local" → Value sourcing: `titulo`. **Verificado em 21/09/2026**: modo `ia` → `"REPARO DE TOMADAS — sala 105"` (várias vezes); modo `manual` (`LLM_ENABLED=false`) → `"Elevador — Elevador social, térreo"` (usa o `tipoServico`, não um nome de catálogo, já que `catalogServiceId` é `null`).
- [x] Descrição do chamado = só as mensagens do solicitante, na ordem, com linha em branco entre elas → Value sourcing: `descricao`. **Verificado em 21/09/2026**: `#CHM-2026-00666` (2 mensagens do solicitante) → `descricao` = as duas mensagens juntas com `\n\n` entre elas, sem nenhuma mensagem da IA misturada.
- [x] Mandar no corpo da ação `catalogServiceId` ou `prioridade` → recusado como dados inválidos → Value sourcing: decisões lidas da proposta. **Fechado na quarta passada (22/09/2026)**: escrevi `shared/conversas/__tests__/abertura.schemas.test.ts` (não existia teste nenhum para este schema), chamando `confirmarAberturaSchema.safeParse(...)` de verdade, em runtime, com um corpo válido mais cada campo proibido por vez. `catalogServiceId`, `prioridade`, `confianca`, `motivo`, `llmCallId` e `modelo` são todos recusados (`success: false`); o corpo mínimo e o modo manual com `tipoServico` continuam aceitos. 8 testes, todos passando. Continua sem forjar a chamada pela rota HTTP real da Server Action (isso exigiria replicar o protocolo Flight do React), mas `safeParse` é o mesmo código que a ação chama, executado de verdade, não lido.
- [x] `grauUrgencia` Normal, natureza Padrão, telefone vazio no chamado do chat → Value sourcing: campos fixos. **Verificado em 21/09/2026**: `#CHM-2026-00667` → `grauUrgencia: "Normal"`, `naturezaAtendimento: "Padrão"`, `telefoneContato: ""`.
- [x] Notificação `ticket:new` para cada Preposto e Admin ativo, uma vez só mesmo com clique duplo → Value sourcing: destinatários da notificação. **Verificado em 21/09/2026** (parcialmente): ao abrir `#CHM-2026-00671`, os 3 registros de `Notification` tipo `ticket:new` criados tinham `userId` exatamente igual aos 3 usuários `Preposto`/`Admin` ativos da base (nem a mais, nem a menos). A parte "uma vez só mesmo com clique duplo" não foi reexercida nesta passada (nenhum clique duplo foi testado neste conjunto de chamados).
- [x] Lista de `/gestao` com dez chamados faz uma consulta só a `DecisaoIa` → Value sourcing: `serviço sugerido pela IA`. **Fechado na quarta passada (22/09/2026)**: já existe `app/api/gestao/chamados/__tests__/route.test.ts`, teste `consulta as decisões uma vez por página, só com os chamados do chat`, que roda a rota de verdade e confere `servicoSugeridoPelaIa` chamada `toHaveBeenCalledTimes(1)` com todos os ids do chat juntos num array só, não um por item; rodei esse teste nesta sessão e ele passou. Combinado com `lib/conversas/proposta-store.ts:437-454`, onde `servicoSugeridoPelaIa` faz um único `DecisaoIaModel.find({ chamadoId: { $in: ... } })`, fica provado que dez chamados (ou qualquer quantidade) na página geram exatamente uma consulta ao Mongo.

## Commands

- [x] `npm run typecheck` → sem erro → todas. **21/09/2026**: `tsc --noEmit` limpo. **Reconfirmado em 22/09/2026** depois do fix dos alvos de toque de 44px: continua limpo.
- [x] `npm run lint` → sem erro → todas. **21/09/2026**: 0 erros, 49 avisos pré-existentes (nenhum nos arquivos desta spec). **Reconfirmado em 22/09/2026**: ainda 0 erros, 49 avisos (mesma contagem; nada novo em `mobile-header.tsx` nem `NotificationsBell.tsx`).
- [x] `MONGO_TEST_URI=mongodb://localhost:27017/severino_test npm test` → tudo passa, inclusive `lib/assistente/__tests__/abertura-chat.db.test.ts` e `lib/conversas/__tests__/proposta.db.test.ts` → AC-1 a AC-17. **Corrigido em 21/09/2026**: a falha era uma corrida em `abertura-chat.db.test.ts` (`LlmCallModel.findOne({})` rodando antes de `recordLlmCall()`, que é fire and forget, terminar de gravar). Ajustado o teste para esperar com `vi.waitFor(...)`, igual ao resto do repo. Suíte completa rodada duas vezes depois da correção: 2193 passaram, 9 pulados, 0 falhas em ambas. **22/09/2026**: rodei os testes novos do fix de alvo de toque (`components/dashboard/__tests__/mobile-header.test.tsx`, `components/realtime/__tests__/`) sozinhos — 2 arquivos, 3 testes, tudo passou. **22/09/2026 (quarta passada)**, depois do fix do AC-14: suíte completa de novo, 2206 passaram, 9 pulados, 0 falhas (2193 da terceira passada mais os 10 testes novos do fix de `afirmaChamadoJaAberto`/`respostaSemAfirmarAbertura` mais os 3 do alvo de toque).
- [x] `LLM_SMOKE=1 npx vitest run lib/assistente/__tests__/abertura.smoke.test.ts` → 8 ou mais acertos em 10 e o relato fora de manutenção com código nulo → AC-19. **Resultado em 18/09/2026, 20h35, fora do pico:** 10 de 10 acertos, latência entre 3,9 e 4,4 segundos por resposta, `finishReason: stop` em todas; relato fora de manutenção com código nulo e `completo: false`. `PROMPT_VERSION` 1, catálogo do seed. (Não rerodado em 21/09; resultado herdado.)
- [x] Na VPS, `docker logs severino-next-app-1 --tail 200 | grep "\[llm\]"` depois de algumas conversas → linhas da tarefa `conversa.abertura` com `task`, `status`, `reason`, `finishReason`, `attempts` e `latencyMs`, sem texto de relato nem chave (herdado da 9) → AC-16. **Verificado em 22/09/2026** (quinta passada, depois do deploy do PR #21): abriu-se de verdade o chamado `#CHM-2026-00389` pelo chat em produção, e `docker logs severino-next-app-1` trouxe várias linhas como `[llm] {"task":"conversa.abertura","lane":"interactive","mode":"stream_object","status":"success","reason":null,"finishReason":"stop","attempts":1,"latencyMs":4893}` — os seis campos exigidos, sem nenhum texto de relato nem chave.
- [x] Na VPS, `docker logs severino-next-app-1 --tail 200 | grep "\[assistente\]\|\[abertura\]"` → só ids, código válido ou não, completo, destino do cartão e duração; nenhuma frase de relato, resposta ou local → AC-16. **Verificado em 22/09/2026**, mesma sessão real: `[assistente] proposta {"conversaId":"6ab29a276bf78efd97378ec9","codigo":"valido","completo":false,"cartao":"nenhum","duracaoMs":4909}` nas voltas sem cartão, virando `"completo":true,"cartao":"novo"` na volta em que o cartão nasceu; e `[abertura] {"conversaId":"6ab29a276bf78efd97378ec9","chamadoId":"6ab29abc6bf78efd97378eec","modo":"ia","jaExistia":false}` na confirmação. Nenhuma das linhas trouxe o texto do relato, da resposta ou do local — só ids, flags e durações, batendo exatamente com o AC-16.

## Acceptance-criteria coverage

- AC-1 · passo 1 da UI, teste `responder.test.ts`, fumaça · AC-2 · passos sem unidade e histórico longo (`responder.test.ts`) · AC-3 · origem dos valores, `abertura-chat.db.test.ts` (fora de ordem) · AC-4 · cartão novo, mantido e substituído · AC-5 · cartão sem confiança, teste "nada vaza" · AC-6 · unidade do perfil, sem unidade, outro prédio · AC-7 · `Revisar e abrir` · AC-8 · IA desligada · AC-9 · cartão manual e classificação · AC-10 · confirmação e dados · AC-11 · modo leitura, notificação, clique duplo · AC-12 · substituído, desatualizado, botão desabilitado · AC-13 · teto de 30 · AC-14 · `prompt-schema.test.ts` · AC-15 · marca em três telas, técnico sem marca · AC-16 · logs na VPS · AC-17 · quatro perfis, corpo estrito · AC-18 · leitor de tela, celular · AC-19 · fumaça
